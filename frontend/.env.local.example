# Copy this file to .env.local for local development.
VITE_API_BASE_URL=http://localhost:8080

# Optional. Required only for manual admin buttons that call protected scheduler endpoints.
# This is embedded in the browser bundle/runtime and is not safe for a public frontend.
VITE_SCHEDULER_API_KEY=
